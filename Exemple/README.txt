To program the microcontroler open the terminal on the folder with Makefile and program file. 
You can configure the device, clock, programmer and fuses in the Makefile.

Program in C
- Write in the terminal 'make flash'
Program in assembler
- Write in the terminal 'make asm'
